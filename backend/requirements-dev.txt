# Dev/test/CI tooling (not installed in the runtime image).
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0          # coverage measurement; CI gate ratchets the floor (never down)
hypothesis>=6.100,<7.0        # property-based tests for the invariant suite (Phase 3)
ruff>=0.6,<1.0
