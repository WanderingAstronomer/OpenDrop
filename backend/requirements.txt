# Runtime dependencies (used by the Docker image and local dev).
# Pinned to compatible ranges: lower bound for features, major ceiling for stability.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
psycopg[binary,pool]>=3.1,<4.0
pydantic>=2.6,<3.0
pydantic-settings>=2.1,<3.0
httpx>=0.27,<1.0
selectolax>=0.3,<1.0
python-dotenv>=1.0,<2.0
